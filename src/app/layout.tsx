import './globals.css';

export const metadata = {
  title: 'FCSE Assessment',
  description: 'Frontend Developer Trial Task',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
